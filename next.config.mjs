/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.noitatnemucod.net",
      "anotherdomain.com",
      "yetanotherdomain.com"
    ]
  }
};

export default nextConfig;
